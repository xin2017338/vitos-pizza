# This repo always records registry.npmjs.org in package-lock.json.
# You can still install via a mirror for speed; lockfile URLs stay official.
replace-registry-host=npmjs

# Default install + publish target (overrides user-level npmmirror while in this repo)
registry=https://registry.npmjs.org/
@vitos-pizza:registry=https://registry.npmjs.org/
