{
	"name": "@vitos-pizza/git",
	"version": "0.4.1",
	"description": "Quick git commit+push via /cp and /bcp for Vito's Pizzeria",
	"type": "module",
	"license": "MIT",
	"private": true,
	"pi": {
		"requires": [
			"@vitos-pizza/subagents"
		]
	},
	"dependencies": {
		"@vitos-pizza/subagents": "*"
	},
	"scripts": {
		"test": "vitest run",
		"test:watch": "vitest",
		"typecheck": "tsc --noEmit",
		"lint": "biome check .",
		"lint:fix": "biome check --write ."
	},
	"peerDependencies": {
		"@earendil-works/pi-agent-core": "*",
		"@earendil-works/pi-ai": "*",
		"@earendil-works/pi-coding-agent": "*",
		"@earendil-works/pi-tui": "*",
		"typebox": "*"
	},
	"devDependencies": {
		"@earendil-works/pi-agent-core": "*",
		"@earendil-works/pi-ai": "*",
		"@earendil-works/pi-coding-agent": "*",
		"@vitos-pizza/subagents": "*",
		"typebox": "*",
		"vitest": "^3.2.4"
	}
}
